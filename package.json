{
  "name": "jev-sim",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "node --env-file=.env scripts/smoke.ts",
    "sensitivity": "node --env-file=.env scripts/sensitivity.ts",
    "baseline": "node --env-file=.env src/main.ts",
    "check": "biome check --write . && tsc && tsc -p viewer",
    "lint": "biome check . && tsc && tsc -p viewer",
    "viewer": "vite viewer",
    "viewer:smoke": "node viewer/smoke/smoke.mjs",
    "viewer:build": "vite build viewer",
    "fork": "node --env-file=.env src/fork.ts",
    "checks": "node --env-file=.env scripts/checks.ts"
  },
  "dependencies": {
    "ai": "^7.0.109",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@types/node": "^26.6.2",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
